$(document).ready(function(){
    $('.your-class').slick({
      // setting-name: setting-value
      autoplay: true,
      autoplaySpeed: 4000,
      arrows: true,
      dots: true,
      // centerMode: true,
      // slidesToShow: 1,
      variableWidth: true,
      // vertical: true,
    });
  });